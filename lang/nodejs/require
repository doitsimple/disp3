^^
var rpath;
if(argv.file){
	rpath = "./" + path.relative(path.dirname(parent.fullpath), argv.file);
	var m =argv.file.match(/([a-z][a-z0-9]+)(?:\.[A-Za-z0-9]+)?$/);
}else if(argv.pkg){
	rpath = argv.pkg;
}else{
// error
}
var sub = argv.sub?"."+argv.sub:"";
var keyname = parent.name || argv.name;

$$
var ^^=$.dash2uc(keyname)$$ = require("^^=rpath$$")^^=sub$$;