^^
var config = argv;
var str = "";
if(config.text) str += "#" + config.text + "\n";
if(!config.min) config.min = "*";
if(!config.hour) config.hour = "*";
if(!config.day) config.day = "*";
str += config.min + " " + config.hour + " " + config.day + " * * cd PROJECT && nohup node " + global.targetDir + "/crons/" + parent.name + ".js";
$$
^^=str$$
