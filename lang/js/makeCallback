^^
var returns = "";
if(argv.returns && argv.returns.length){
	argv.returns.forEach(function(e){
	  returns += ", "+Object.keys(e)[0];
  });
}
$$
function(err^^=returns$$){
	if(err){
		^^=~argv.error$$
	}else{
		^^=~argv.success$$
	}
	^^=~argv.content$$
}