^^
var args = "";
if(parent.args){
	if(typeof parent.args == "string")
		args += ", "+parent.args;
	else
		args += ", "+Object.keys(parent.args).join(", ");
}
$$
function(err^^=args$$){
	if(err){
		^^=~argv.error$$
	}else{
		^^=~argv.success$$
	}
	^^=~parent.content$$
}