^^
if(!argv.error){
	argv.error = {error: "err"};
}
if(!argv.success){
	argv.success = {"success": 1};
}

var args = "";
if(parent.args){
	if(typeof parent.args == "string")
		args += ", "+parent.args;
	else
		args += ", "+Object.keys(parent.args).join(", ");
}
$$
function(err^^=args$$){
	if(err){
		^^=~argv.error$$
	}else{
		^^=~argv.success$$
	}
	^^=~parent.content$$
}