^^
var str = Object.keys(argv).join(" ");
var str = "";
var lastkey = $.lastkey(argv);
for(var key in argv){
	var tmp = {};
	tmp[key] = argv[key];
	str += $.uc2dash(key);
	if(key != lastkey)
		str += " ";
}
$$
class="^^=str$$"