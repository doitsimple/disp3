^^for(var key in global.impl.crons){$$
^^=makeCronStr(global.impl.crons[key])$$
^^}$$

^^function makeCronStr(config){
	var str = "";
	if(config.text) str += "#" + config.text + "\n";
	if(config.type == "raw") str+= config.frequency + " " + config.commands + "\n";
	return str;
}$$